# bacalhau Environment Configuration
# Copy this file to .env and customize as needed

# App version (shown in UI)
VITE_APP_VERSION=1.0.0

# Development mode
NODE_ENV=development

# CORS Proxy URL (for accessing external streams)
VITE_PROXY_URL=http://localhost:8888

# Transcoder service URL
VITE_TRANSCODER_URL=http://localhost:3001

# Recordings storage path (used by docker-compose)
# Set this to customize where recordings are saved
RECORDINGS_PATH=./recordings
# Playlists storage path (used by docker-compose)
# Playlists are stored on disk for persistence across devices
PLAYLISTS_PATH=./playlists